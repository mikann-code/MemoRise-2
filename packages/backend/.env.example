# ローカル（Docker を使わず直接 rails s）で使う環境変数。
# .env にコピーして使う。
DATABASE_HOST=127.0.0.1
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=password
FRONTEND_URL=http://localhost:3200
LOCAL_FRONTEND_URL=http://localhost:3200
