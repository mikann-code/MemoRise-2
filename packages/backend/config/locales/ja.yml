ja:
  hello: "こんにちは"

  # バリデーションエラーの項目名を日本語で表示する。
  # メッセージ本体（「を入力してください」等）は rails-i18n が提供する。
  # 例: メールアドレス + はすでに存在します → 「メールアドレスはすでに存在します」
  activerecord:
    attributes:
      user:
        name: 名前
        email: メールアドレス
        password: パスワード
