# スクリーンショット置き場

README で参照する画面キャプチャを配置する。

- 命名は `<画面名>-<連番>.jpg`（例: `top-01.jpg` / `wordbook-01.jpg` / `test-01.jpg` / `dashboard-01.jpg`）。
- 実機（iPhone / Safari）で本番環境を撮影。README 側は `<img width="200">` で横並びに表示する。
- 実在のメールアドレスなど個人情報が写り込んだものは置かない（デモ用アカウントで撮影する）。
